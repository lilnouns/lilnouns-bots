{
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env dev",
		"dev": "wrangler dev --env dev --test-scheduled"
	},
	"devDependencies": {
		"wrangler": "3.100.0"
	},
	"engines": {
		"node": ">=16.13.0",
		"pnpm": ">=9.0.0"
	}
}
