{
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env dev",
		"dev": "wrangler dev --env dev --test-scheduled"
	},
	"devDependencies": {
		"wrangler": "3.13.2"
  },
  "engines": {
    "node": ">=16.13.0",
    "pnpm": ">=8.0.0"
  }
}
