{
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env dev",
    "dev": "docker-compose up --build"
	},
	"devDependencies": {
		"wrangler": "3.11.0"
  },
  "engines": {
    "node": ">=16.13.0",
    "pnpm": ">=8.0.0"
  }
}
